apply plugin: 'kotlin'

dependencies {
    implementation rootProject.ext.domainImplementationDependencies.values()
    testImplementation rootProject.ext.domainTestImplementationDependencies.values()
}
